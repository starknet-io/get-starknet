# Wallets

The `@get-starknet/wallets` package provides a list of all Starknet wallets.

You can use this list to build a UI that suggests wallets to the user.

## Usage

```ts
import { wallets } from "@get-starknet/wallets";
```